name: popper
description: Floating UI and Popper-style positioning for Dart web apps, with middleware, auto update, portals, and anchored overlays.
version: 2.0.0
repository: https://github.com/insinfo/popper_dart.git
issue_tracker: https://github.com/insinfo/popper_dart/issues
topics:
  - popover
  - tooltip
  - overlay
  - position
  - web

environment:
  sdk: ^3.6.2


dependencies:
  web: ^1.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
